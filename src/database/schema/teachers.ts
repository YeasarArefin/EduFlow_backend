import { sql } from "drizzle-orm";
import { bigint, index, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid, varchar } from "drizzle-orm/pg-core";
import { workspaces } from "./workspaces";
export const teacherStatus = pgEnum("teacher_status", ["active", "inactive", "archived"]);
export const teachers = pgTable("teachers", { id: uuid("id").defaultRandom().primaryKey(), workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id), teacherCode: varchar("teacher_code", { length: 30 }).notNull(), name: varchar("name", { length: 150 }).notNull(), phone: varchar("phone", { length: 30 }), email: varchar("email", { length: 255 }), subjectSpecialty: varchar("subject_specialty", { length: 150 }), defaultSalaryMinor: bigint("default_salary_minor", { mode: "bigint" }).notNull().default(sql`0`), status: teacherStatus("status").notNull().default("active"), notes: text("notes"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (table) => [uniqueIndex("teachers_workspace_code_idx").on(table.workspaceId, table.teacherCode), index("teachers_workspace_status_idx").on(table.workspaceId, table.status), index("teachers_workspace_name_idx").on(table.workspaceId, table.name)]);
